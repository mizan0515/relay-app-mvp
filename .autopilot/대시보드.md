# 🤖 자동개발 로봇 대시보드 (관리자용)

> **이 파일은 매 iteration마다 로봇이 자동으로 갱신합니다.**
> 관리자님은 이 파일 하나만 보시면 됩니다. 다른 파일은 열 필요 없어요.

---

## 📌 지금 상태 (한눈에)

- **마지막 업데이트:** 2026-04-19 (iter 73 · **idle-upkeep streak=6** · R-REVIEW 지속)
- **로봇 상태:** 👀👀 이중 리뷰대기 지속 — PR #53(validator) + PR #57(CLAUDE.md 서두 v2) 관리자 승인 대기
- **진행률 (MVP 게이트):** **7/8** · [x] B1 · [👀] validator PR #53 · [x] B3 완료 · [👀] CLAUDE.md PR #57
- **열린 PR:** 2건 (#53, #57) · 누적 머지 **28건** · idle_upkeep_streak **6** · 승인 도착 시 즉시 G1 flip

---

## 👉 관리자님이 지금 해주실 일

## 👀 관리자님께 드리는 제안

> **✅ G1 3결정 접수 완료 (iter61)**: 관리자님이 *"전부 추천대로"* 지시 → `1:b · 2:e · 3:승인` 확정.
> 로봇은 iter62 부터 **G1 스프린트**(2~4 iter) 를 돌려 MVP **8/8** 완주를 시도합니다.
>
> 남은 관리자 대기 항목은 **B14**(tools/smoke A/B/C) 와 **B10**(blocked-state 규칙 배선) 2건.
> 둘 다 G1 과 독립이라 **답변 없어도** 로봇은 G1 스프린트를 진행합니다.

**iter62 계획 4단계**:
1. **B1 — PacketIO.Read/Write** — `CodexClaudeRelay.Core/Protocol/PacketIO.cs` 신규 + `CodexClaudeRelay.Core.Tests` 라운드트립 xunit (`autopilot/g1-peer-symmetric-packet-io` 브랜치, 자동 머지 후보).
2. **validator 포팅 (b)** — `D:\dad-v2-system-template\{en,ko}\tools\Validate-Dad*.ps1` 을 참고해 `tools/Validate-Dad-Packet.ps1` 착륙 (protected → 한국어 PR, 관리자 리뷰 필수).
3. **B3 — AgentCostAdvisor 대칭화 (e)** — Claude-only cost 브랜치를 Codex/Claude 대칭 전략으로 묶기 (Core 내부, 자동 머지).
4. **CLAUDE.md 서두 정정 (승인)** — 상단 ~20줄 v2 언어로 교체 (protected → 한국어 PR).

---

**iter73 — idle-upkeep streak=6 · R-REVIEW 지속.** PR #53/#57 여전히 무변경(OPEN, no review, no comment). 코드·테스트·누적 머지 불변(96/96 · 28건). 관리자 승인 한 줄이면 즉시 머지 → G1 `[x]` flip → **MVP 8/8 완주**.

(이전) **iter72 — idle-upkeep streak=5 · R-REVIEW 지속.** PR #53/#57 여전히 동일(OPEN, no review, no comment). R-REVIEW 정책상 streak 상관없이 idle-upkeep 유지. 코드·테스트·누적 머지 불변(96/96 · 28건). 관리자 승인 한 줄이면 즉시 머지 → G1 `[x]` flip → **MVP 8/8 완주**.

(이전) **iter71 — idle-upkeep streak=4 · R-REVIEW 지속, 폴링 무변경.** iter71 boot 폴링에도 PR #53/#57 전부 `state=OPEN` / `reviewDecision=""` / `comments=0` 그대로. R-REVIEW 분류상 halt 규칙 없음 — streak 이 얼마나 늘어도 idle-upkeep 유지가 정책. iter70 에서 기록한 Brainstorm 스냅샷(B16/B17/B18) 은 전부 `protected_paths` 라 현재 착수 불가, 관리자 승인 후 순차 진행 예정. 코드·테스트·누적 머지 전부 불변(96/96 · 28건). 관리자님이 두 PR 중 하나에 "승인" 한 줄만 남기시면 즉시 머지 → G1 `[x]` flip → **MVP 8/8 완주**.

(이전) **iter70 — idle-upkeep streak=3 · Brainstorm 스냅샷 기록 + 폴링 무변경.** iter70 boot 폴링: PR #53/#57 동일하게 `state=OPEN` / `reviewDecision=""` / `comments=0`. B10 `DECISION-BLOCKED-STATE.md` §5 재확인 — **이 케이스는 R-BLOCKED 가 아니라 R-REVIEW**(정책 차단, 원인 명확: protected_paths 한국어 리뷰 규칙 준수). R-REVIEW 는 자동 Brainstorm 전환 대상이 아니므로 idle-upkeep 유지. 그래도 Brainstorm 정신에 맞춰 **비코드 스냅샷** 기록: 운영자 복귀 시 즉시 착수 가능한 후속 후보 3건 — **B16** (AGENTS.md 서두 ~20줄 v2 정정, CLAUDE.md 와 대칭 · Codex Agent Contract 버전) · **B17** (PROJECT-RULES.md 서두 v2 정정, "template source repository" 문구 교체) · **B18** (세 문서 본문에 남은 `en/` · `ko/` 참조 일괄 정리 — 별도 루트 저장소이므로). 셋 모두 `protected_paths` 라 본 iter 에서 PR 생성 불가. 코드베이스 상태 점검: Core 빌드 경고 0, 전체 TODO/FIXME 0, RelayBroker 1900→1817 LOC (B3 전체 이사로 -83 LOC). 자동 머지 가능한 대안 경로는 여전히 0건(B8 protected+xunit 중복, 신규 후보 셋 다 protected). 코드·테스트·누적 머지 전부 불변(96/96 · 28건). 관리자님이 "승인" 한 줄만 남겨주시면 즉시 둘 다 머지 → G1 `[x]` flip PR → **MVP 8/8 완주**. iter71 도 폴링 지속(streak=4), streak=5 이상도 R-REVIEW 케이스라 halt 전환 없이 idle-upkeep 유지.

(이전) **iter69 — idle-upkeep streak=2 · 리뷰 2건 여전히 무변경.** iter69 boot 폴링에도 PR #53·#57 둘 다 `state=OPEN` / `reviewDecision=""` / `comments=0` 유지. idle_upkeep_streak 1 → 2. 다음 iter70 에서 3 도달 시 **B10 R-BLOCKED** 규칙(`.autopilot/DECISION-BLOCKED-STATE.md`) 에 따라 Brainstorm 모드로 자동 전환 — 운영자 리뷰 지연 원인 탐색 + BACKLOG 신규 후보 발굴 루프 실행. 병행 코드 개선 재확인: B8 (protected + xunit 중복) / AGENTS.md · PROJECT-RULES.md 서두 드리프트 (protected, 운영자 승인 범위 외) — 자동 머지 가능한 작업 여전히 0건. 코드·테스트·누적 머지 전부 불변(96/96 · 28건). 두 PR 중 하나라도 관리자님이 "승인" 남기시면 즉시 머지 → G1 `[x]` flip PR → **MVP 8/8 완주**.

(이전) **iter68 — idle-upkeep · 리뷰 대기 2건 폴링 무변경.** iter68 boot 폴링 두 건 모두 무변화: PR #53 (validator, 7일째) state=OPEN/reviewDecision=""/comments=0, PR #57 (CLAUDE.md 서두, 1 iter 경과) 동일. 둘 다 `protected_paths` 포함이라 로봇 자동 머지 불가 → 관리자 한국어 리뷰 필수. 병행 개선 후보 재검토: **B8** (tools/run-smoke.ps1 headless smoke) 는 (a) `tools/` 도 protected → 자동 머지 불가, (b) G4/G5/G6/G8 xunit 이 이미 end-to-end round-trip + rotation + dedup 을 커버 → 실익 희박, BACKLOG 주석에 "re-scope or drop" 이미 기록. 그 외 AGENTS.md · PROJECT-RULES.md 에도 en/ko 드리프트 잔재 있으나 protected 라 이번 iter 운영자 승인 범위 밖. 결론: **병행 코드 작업 없음** — idle-upkeep 모드로 진입. 코드·테스트 무변경(96/96 유지), 누적 머지 28건 유지. idle_upkeep_streak `1` 로 증가. iter69 도 동일 폴링 지속. 3 iter 연속 무변경(iter70) 시 B10 R-BLOCKED 규칙(PROMPT.md 예고) 에 따라 Brainstorm 모드로 전환 후보 — 그때 운영자 리뷰 지연 원인 탐색 + 신규 백로그 발굴 루프 실행. 머지되면 즉시 G1 `[x]` flip PR → **MVP 8/8 완주**.

(이전) **iter67 — G1 4/4 마지막 단계 PR #57 오픈 (CLAUDE.md 서두 v2 정정) · 관리자 리뷰 대기 2건.** iter67 boot 폴링: PR #53 state=OPEN, reviewDecision="", comments=[] — 엿새째 관리자 리뷰 미도착. B3 완료 직후 G1 마지막 step 4 착수. `CLAUDE.md` 서두 ~20줄(iter61 operator 승인 범위) 에서 v1 drift 표현 정리: (a) 맨 위 소개 — "template source repository for en/ko variants" → "DAD-v2 peer-symmetric relay (C# .NET); 이 저장소는 템플릿이 아니고 Claude Code 는 audit-only 가 아님", (b) Role 섹션 — "Claude maintains the template" → "Claude participates as a peer (Codex ≡ Claude Code)", (c) must-rules 에 "역할별 비대칭 분기 금지" 추가해 iter64~66 B3 결과(`Policy/IAgentCostAdvisor.cs`) 와 문서-코드 링크 형성, (d) must-not 에 audit-only 자칭 금지 · 비대칭 분기 금지 · en/ko 변형 가정 금지. `## Source Repo Workflow` · `## Related Files` 섹션은 **한 글자도 미변경**(iter61 operator scope "본문 유지" 엄수). 본문에 남은 en/ko 참조는 별도 iter 후속 정리 후보(이번 PR 범위 밖 명시). `protected_paths` → PR #57 관리자 한국어 리뷰 필수. PR 본문: "이 파일 서두가 현재 저장소와 맞지 않는 상태(드리프트) 였어요" 를 비개발자 한 줄로 풀이 + 승인/수정요청 댓글 예시. 코드·테스트 무변경(96/96 유지), 누적 머지 28건 유지. **리뷰 대기 PR 2건(#53·#57)** — 둘 다 관리자 승인 머지되면 G1 `[x]` flip → **MVP 8/8 완주**. iter68+ 는 리뷰 대기 병행 idle-upkeep; 리뷰 무변경 3 iter 연속이면 B10 R-BLOCKED 규칙 trigger 후보.

(이전) **iter66 — G1 3/4 step 3 완료 · PR #56 MERGED (ClaudeCostAdvisor 이사) · B3 전체 완료 🎉 · PR #53 여전히 리뷰대기.** iter66 boot 폴링: PR #53 state=OPEN, reviewDecision="", comments=[] — 닷새째 관리자 리뷰 미도착. 병행 개선으로 **B3 step 3 = 마지막 단계** 착수. 브로커 안에 남아 있던 Claude 전용 비용 메서드 4개(`LogCostAvailabilitySignalsAsync` · `LogCacheInflationSignalAsync` · `LogClaudeCostCeilingDisabledAsync` · `TryRotateForClaudeCostCeilingAsync`)를 `Policy.ClaudeCostAdvisor` 로 이사. 설계 핵심: **`IBrokerCostContext` 좁은 창구를 3가지 확장** — `Options` (설정 읽기) + `TripBudgetAsync` (예산 트립 로그) + `RotateSessionAsync` (세션 회전). 이렇게 하면 Claude 조언자가 브로커 전체가 아니라 이 네 가지 좁은 멤버만 보면서 전체 회전 플로우를 스스로 완결할 수 있음. `RelayBroker` 는 explicit interface implementation 으로 셋을 연결 — 외부 공개 API 변화 전혀 없음. 브로커 호출지점은 5줄 → **2줄 단일 디스패치**(`advisor.OnUsageObservedAsync` + `advisor.ShouldRotateForCostCeilingAsync`). `SuspiciousClaudeCacheCreationTokens` private const 도 브로커 → 조언자로 이사. **이벤트 타입·메시지·`State` 플래그 이름·트립/회전 순서 모두 byte-identical**. 테스트 6건 신규 + `RecordingCostContext` 확장(Options + RotateCallCount + TripBudget 훅): 빈 usage silent · cost-absent 1회·양의 비용 skip · ceiling-disabled 1회 (non api-key) · rotate at ceiling (api-key, 상한 초과) · no-rotate below ceiling. 전체 91 → **96 tests**(+5 순증). 빌드 경고 0, 자동 머지(22aecae, non-protected). 누적 머지 27 → **28**. **B3 3단계 모두 완료** — Codex/Claude 비용 처리가 이제 완전 대칭(각자 같은 모양의 Advisor 한 쌍). iter67 에서 G1 step 4 — `CLAUDE.md` 서두 ~20줄을 v2 peer-symmetric 언어로 정정(protected → 한국어 PR · 관리자 리뷰 필수). 그 PR 머지되면 G1 `[x]` flip → **MVP 8/8 완주**.

(이전) **iter65 — G1 3/4 step 2 완료 · PR #55 MERGED (CodexCostAdvisor 이사) · PR #53 여전히 리뷰대기.** iter65 boot 폴링: PR #53 state=OPEN, reviewDecision="", comments=[] — 나흘째 관리자 리뷰 미도착. 리뷰 대기 병행으로 **B3 step 2** 착수 — Codex 전용 비용 안내 메서드 2개(`LogCodexPricingFallbackAsync` 가격표 대체 + `LogCodexRateCardStaleAsync` 오래된 가격표)를 `Policy.CodexCostAdvisor.OnUsageObservedAsync` 로 이사. 핵심 설계: **`IBrokerCostContext` 좁은 창구** 신설 — 조언자가 브로커 전체를 보지 않고 `RelaySessionState State` + `PersistAndLogAsync` 두 가지만 쓸 수 있게 제한. `RelayBroker` 는 `IBrokerCostContext` 를 명시적 인터페이스 구현(explicit interface implementation)으로 연결 → 외부 API 면적 무변화. 기존 호출지점 2줄을 `_costAdvisors.For(role).OnUsageObservedAsync(this, effectiveUsage, ct)` 단일 디스패치 1줄로 교체. **이벤트 타입·메시지·`State` 플래그 이름 모두 byte-identical**(기존 로그 파이프라인/테스트 불변). 테스트 4건 신규 + `RecordingCostContext` 헬퍼(가격표 대체 1회 발사 + 빈 이유 무발사 + 멱등성 + 오래된 가격표 1회 발사). 전체 89 → **91 tests**(+2 순증, step 1 no-op `[Theory]` 2건이 실제 계약 테스트로 승격). 빌드 경고 0, 자동 머지(550934d, non-protected Core+Tests). 누적 머지 26 → **27**. iter66 step 3 에서 Claude 쪽 4 메서드(cost-ceiling-disabled · cache-inflation · cost-absent · TryRotateForClaudeCostCeiling) 이사 → B3 완료 → CLAUDE.md 서두 정정(관리자 리뷰) → G1 `[x]` → **MVP 8/8**.

(이전) **iter64 — G1 3/4 step 1 완료 · PR #54 MERGED (IAgentCostAdvisor scaffold) · PR #53 여전히 리뷰대기.** iter64 boot 폴링: PR #53 state=OPEN, reviewDecision 빈 값, comments=[]. 리뷰 대기 병행 개선으로 B3 착수 — 하지만 `RelayBroker.cs`(1900 LOC) 의 6개 role-conditional 비용 메서드는 State/_options/PersistAndLogAsync/TripBudget/RotateSession 같은 broker 내부 가족을 공유하므로 한 번에 옮기면 리뷰 부담과 회귀 가능성이 큼. 그래서 **3단계 나누기** 전략: **step 1 = scaffold(이번 PR)**, step 2 = Codex 2 메서드 이사, step 3 = Claude 4 메서드 이사. 이번 PR #54 는 **동작 불변**의 타입 수준 seam 만 착륙 — `IAgentCostAdvisor` 인터페이스(default no-op 2 훅) + `AgentCostAdvisorRegistry`(쌍 생성자로 Codex+Claude 둘 다 등록 강제 → 대칭성을 타입 수준에서 보증) + `CodexCostAdvisor`/`ClaudeCostAdvisor` 빈 구현체. xunit 5건: 매칭 lookup · swapped 거부(Claude를 codex 슬롯) · swapped 거부(Codex를 claude 슬롯) · unknown role 거부 · [Theory] codex/claude 둘 다 default 훅 no-op 확인. RelayBroker 한 줄도 안 건드림 → 기존 84개 테스트 전부 그대로 통과, 테스트 84 → **89** (+5). 빌드 경고 0, 자동 머지(58b6974). 누적 머지 25 → **26**. iter65 에서 step 2 (Codex 2 메서드 CodexCostAdvisor 로 이사) 예정, Core 내부라 또 자동 머지 가능. 여전히 PR #53(validator, protected) 은 관리자 한국어 리뷰 대기.

(이전) **iter63 — G1 스프린트 2/4 단계. PR #53 오픈(validator 포팅 · 관리자 리뷰 대기).** 관리자 iter61 지시대로 option **(b)** — `D:\dad-v2-system-template\en\tools\Validate-DadPacket.ps1` (780 LOC) 에서 **turn-packet 자체 검증 규칙만** 뽑아 루트 `tools/Validate-Dad-Packet.ps1` (242 LOC) 로 착륙. state.json · summary · prompt_artifact 파일 존재 같은 라이브-세션-워크스페이스 전용 규칙은 제거 — CLAUDE.md "루트는 template source repo, 라이브 세션 아님" 계약 준수. iter62 C# `PacketIO` 라운드트립 불변식과 쌍을 이루는 CLI 검증기: 필수 top-level · 금지 root 필드 · from enum · closeout_kind 조건부 규칙 · checkpoint status enum. 수동 스모크 검증 완료(합법 fixture pass / 4-위반 fixture 정확히 4 issue + exit 1). `tools/` 는 protected_paths → **PR #53 관리자 한국어 리뷰 필수**. PR 본문은 "이게 뭐 하는 건가요" · "왜 풀버전 안 옮겼나요" · 검증 규칙 체크리스트 포함. iter64 부터는 리뷰대기 병행으로 G1 3단계(B3 AgentCostAdvisor 대칭화) 착수 — Core 내부라 자동 머지 가능. 코드·C# 테스트 무변경(84/84 유지), 누적 머지 25건 유지.

(이전) **iter62 — G1 스프린트 1/4 단계 완료! PR #52 MERGED (B1 PacketIO round-trip).** `CodexClaudeRelay.Core/Protocol/PacketIO.cs` 신규 — 이미 있던 write-only `TurnPacketYamlPersister` 위에 얇은 facade 로 `Read/Write` 한 쌍. YamlDotNet 없이 Persister 결정적 포맷을 line-walk 로 역파싱 → Core 프로젝트 NuGet 의존 **0건** 유지. 테스트 3건 추가: (1) [Theory] `from: codex` / `from: claude-code` 양쪽 대칭 round-trip — 콜론·따옴표·역슬래시·여러 개 questions·여러 checkpoint_results 포함, (2) 빈 questions/checkpoint_results 리스트 라운드트립. 빌드 경고 0, 전체 **84/84** (81 +3). PR #52 자동 머지(5e916d6, non-protected). 누적 머지 24 → **25**. 다음 iter63 = validator 포팅 (b) — en/ko 템플릿의 `Validate-Dad*.ps1` 을 스크래핑해 `tools/Validate-Dad-Packet.ps1` 착륙 (protected path → 한국어 PR · 관리자 리뷰 필수). 그 다음 iter64+ = B3 AgentCostAdvisor 대칭화 → CLAUDE.md 서두 정정 → G1 `[x]` → **MVP 8/8**.

(이전) **iter61 — 🎉 관리자 G1 3결정 답변 접수! "전부 추천대로" → 1:b · 2:e · 3:승인.** 관리자님 원문: *"전부 추천하는대로 해줘 무슨뜻인지 못알아먹겠어 쉽게 풀어서 설명해줘"*. 로봇 대응 — ① 각 결정을 비개발자 고등학생용 한 줄 풀이로 재설명(validator=자동 채점기·대칭화=두 에이전트 동일 지갑 체크·서두 정정=v1 흔적 용어 교체), ② `.autopilot/G1-UNBLOCKING-RUNBOOK.md` 하단에 "관리자 답변 (iter61)" 블록 추가해 결정 영구 기록 + iter62+ 4-step 계획 명시, ③ `.autopilot/OPERATOR-PENDING-INDEX.md` §2 🟠→✅ 로 승격, ④ STATE `parked_task` 엔트리를 `active_task_g1` 로 승격(branch 예약·decisions 3필드 기록) + `operator_requests` 비움 + `operator_answered_iter61` 기록. iter62 계획: B1 PacketIO 브랜치 → validator (b) 스크래핑 PR → B3 AgentCostAdvisor (e) → CLAUDE.md 서두 PR → G1 [x] flip → **MVP 8/8**. 코드·테스트 무변경(문서 4건만), 81/81, 누적 머지 24건 유지. 리뷰대기 0건, G1 언블록 된 채 iter62 착수.

(이전) **iter60 — PR #51 MERGED! 🎉 · B15 Flip digest MVP-GATES.md 상단 landed (관리자님 감사합니다).** iter60 폴링 6회째에 `"state":"MERGED"` 확인 — 자동 머지가 아닌 **관리자님이 승인 머지**. commit `f21b617`. MVP-GATES.md 상단에 8-게이트 Flip digest 표가 main 에 착륙. 누적 머지 **23→24건**. `OPERATOR-PENDING-INDEX.md` §1 ✅ 완료로 갱신, 남은 대기 항목 G1 3결정 · B14 · B10 3건으로 축소. BACKLOG B15 `[DONE iter60]`. 리뷰대기 해제 → 다음 iter61 부터 idle-upkeep 모드 재진입. 코드·테스트 무변경(문서 머지만), 81/81, MVP 7/8 유지. 남은 MVP 경로는 여전히 G1 (`.autopilot/G1-UNBLOCKING-RUNBOOK.md` 3결정 도착 대기).

(이전) **iter59 — PR #51 폴링 5회(변화X) + OPERATOR-PENDING-INDEX.md 착륙(한 페이지 인덱스).** 폴링 여전히 state=OPEN/reviewDecision=""/comments=[]. 병행 개선으로 iter51·52·58 에 걸쳐 각각 흩어져 있던 운영자 대기 항목(PR #51 리뷰 · G1-UNBLOCKING-RUNBOOK 3결정 · ADR-TOOLS-SMOKE-NEED B14 A/B/C · DECISION-BLOCKED-STATE R-BLOCKED/R-REVIEW/R-DRIFT)을 `.autopilot/OPERATOR-PENDING-INDEX.md` 한 파일로 통합. 각 항목마다 🔴/🟠/🟡 우선순위 + "어느 파일" + "무슨 한 줄 답변" 만 표기 — 관리자 로딩 비용 최소화. "관리자님께 드리는 제안" 섹션 상단 안내문도 인덱스 링크로 단순화. MVP 7/8, 81/81, 누적 23건. 코드·테스트 무변경.

(이전) **iter58 — PR #51 폴링 4회째(변화 없음) + B10 spec 착륙(DECISION-BLOCKED-STATE.md).** 폴링: state=OPEN, reviewDecision="", comments=[]. 변화 없음. 병행 개선으로 **B10**(원안은 `.autopilot/PROMPT.md` 직접 수정 → protected 차단) 의 **대체 경로** 수행 — 규칙 스펙을 non-protected 문서로 먼저 착륙시켜 운영자 승인 후 protected 배선. 신규 `.autopilot/DECISION-BLOCKED-STATE.md` 에 세 규칙 제안: **R-BLOCKED**(operator 블록 지속 시 +3 iter Brainstorm / +5 iter Idle-upkeep / +10 iter Halt), **R-REVIEW**(리뷰 대기는 별개 분류 — iter54~57 가 이 케이스), **R-DRIFT**(3 iter 연속 STATE/대시보드/METRICS 외 무변경 시 경고). 각 규칙은 iter25/32/50~57 실제 행동과 대조 검증. 응답 양식(`R-BLOCKED: 승인/수정요청/보류`) 포함 — 승인 시 iter59+ 에서 `.autopilot/PROMPT.md` PR 로 배선. BACKLOG B10 `[SPEC-LANDED iter58]` 로 상태 변경. MVP 7/8, 81/81, 누적 23건.

(이전) **iter57 — PR #51 폴링 3회째(변화 없음) + B13.1 regen-histogram.py 착륙(병행 개선).** 먼저 폴링: `gh pr view 51`: state=OPEN, reviewDecision="", comments=[]. 변화 없음. 리뷰 대기 중 낭비 방지 차원으로 **B13.1 follow-up** 수행 — `.autopilot/regen-histogram.py` (stdlib only, 65 LOC) 신규. `python3 .autopilot/regen-histogram.py` 실행 시 대시보드 "📈 진행 히스토그램" 블록(6h 버킷 ASCII + flip 연표 표) 을 stdout 으로 재생성 → 차후 exit-contract 훅에 붙일 수 있는 확장 포인트 확보. 실행 검증 완료(METRICS 55줄 파싱 → 4 버킷 + 5 flip 라인 출력). non-protected 직접 main. MVP 7/8, 81/81 유지. BACKLOG B13.1 `[DONE iter57]` 마킹.

(이전) **iter56 — PR #51 폴링 2회째, 여전히 변화 없음.** `gh pr view 51 --json ...comments`: state=OPEN, reviewDecision="", mergeStateStatus=UNKNOWN, comments=[], statusCheckRollup=[]. 관리자 리뷰·댓글 모두 미도착. 리뷰 대기는 차단 사유가 명확(protected_paths 정책) 하므로 PROMPT.md halt 규칙 대상 아님(drift 가 아니라 정상 정책 차단). iter57 에서 3회째 폴링, 변화 없으면 idle-upkeep 병행 재개하여 작은 개선(B11/B13 follow-up 등) 지속. MVP 7/8, 81/81, 누적 23건 유지. 코드·파일 변경 없음.

(이전) **iter55 — PR #51 폴링 1회째, 상태 변화 없음.** `gh pr view 51` 결과: state=OPEN, reviewDecision 빈 값, mergeStateStatus=UNKNOWN, statusCheckRollup 비어있음. PR 은 문서 1건(protected_paths 내 `.autopilot/MVP-GATES.md`)만 수정이라 CI 체크 대상 없고, 실제 머지 차단 요인은 **관리자 한국어 리뷰 도착 여부**. 자동 머지 금지(영구 operator directive 준수). 이번 iter 는 상태 확인 + 대시보드 기록만 수행. 다음 iter56 에서 폴링 2회, iter57까지도 변화 없으면 PROMPT 규칙대로 idle-upkeep 또는 halt-soft 검토. MVP 7/8, 81/81, 누적 머지 23건 유지.

(이전) **iter54 — PR #51 생성(B15 Flip digest · 관리자 리뷰 대기).** `.autopilot/MVP-GATES.md` 문서 최상단(Regression protocol 직후 · G1 직전)에 **"Flip digest"** 섹션 추가 — 8개 게이트별 한 줄 표(현재 상태 / 최신 flip 날짜·PR·commit / 근거 한 줄). 아래 개별 게이트 본문과 기존 Flip history 는 **전량 보존**(regression audit 규칙). 파일 변경 +19/-0. protected_paths 라 자동 머지 불가 → 관리자 한국어 리뷰 필수 (PR 본문은 비개발자 기준 한국어로 작성, "바뀌는 것 / 그대로인 것 / 왜 필요한가요" 섹션 포함). 부속 결정: **B11(EventHashStamper 리팩터) CLOSED** — iter48 에 이미 `JsonlEventLogWriter.ComputeEventHash` 단일 함수로 중앙화 완료, 래퍼 1단계 추가는 간접 참조만 늘리고 실익 없음으로 재분석. BACKLOG 반영. idle_upkeep_streak 4→0 리셋(실제 PR 생성). 코드·테스트 무변경(문서 1파일만), 81/81 유지.

(이전) **iter53 — idle-upkeep. B13 진행 히스토그램 착륙(대시보드 하단 신설).** `.autopilot/METRICS.jsonl` 53줄을 파싱해 (a) 6시간 구간별 iter/머지 수 ASCII 히스토그램 + (b) 게이트 flip 연표 표를 대시보드 최하단 "📈 진행 히스토그램" 섹션으로 신설. 관찰 한 줄 — 04-18 12h 구간에 G7 1건 착륙 후, 04-19 자정 이후 G4·G5·G6·G8 **연속 4 flip**. 핵심 인프라(BrokerCwdMutatingCollection, fake-adapter) 확보 시점에 스프린트 속도가 붙었다는 사실이 숫자로 드러남. 옛 누적머지 10건 문구는 23건으로 보정. BACKLOG B13 `[DONE iter53]`, STATE iter52→53(idle_upkeep_streak 3→4). 재생성은 수동(필요 시 `python3` 한 블록) — 자동 파이프라인은 B13.1 후속으로 미룸. 코드·테스트 무변경, 81/81 유지, 누적 머지 23건 유지, non-protected 직접 main.

(이전) **iter52 — idle-upkeep. B14 ADR 착륙(tools/smoke 래퍼 필요 여부).** `.autopilot/ADR-TOOLS-SMOKE-NEED.md` 신규 — G4·G5·G6·G7·G8 e2e 가 이미 xunit 으로 브로커 round-trip / recovery / rotation / convergence / dedup 을 커버하는 사실 점검표 + 세 선택지(**A** 5 LOC `dotnet test` 래퍼 / **B** 원안 full PS harness / **C** 드랍) 대비표 + 운영자 응답 양식(`B14: A/B/C`). 권고는 **Option A** — 중복 없이 CLI 편의+CI 진입점만 확보. `tools/` 는 protected 라 승인 도착 시 iter53+ 에서 착수, 답 없으면 Option C 로 간주하고 B14 영구 닫음. BACKLOG B14 `[DONE iter52]` 마킹, STATE iter51→52(idle_upkeep_streak 2→3). 코드·테스트 무변경, 81/81, 누적 머지 23건. 이 파일(.autopilot/ADR-*.md) 은 non-protected → main 직접 커밋.

(이전) **iter51 — idle-upkeep. 관리자용 G1 결정서 착륙.** 코드 변경 0, 문서 1건 신규 — `.autopilot/G1-UNBLOCKING-RUNBOOK.md` 를 작성해 운영자님이 편한 시간에 열어보고 **세 가지 결정**(validator 포팅 a/b/c · 비용 경로 d/e/f · CLAUDE.md 서두 정정 승인/보류)을 한 줄 응답으로 주실 수 있게 정리. 각 결정마다 "무엇이 바뀌나요 / 무엇이 그대로인가요 / 비용 몇 iter" 표로 풀어 썼고, 맨 아래 `1:(a/b/c) 2:(d/e/f) 3:(승인/보류)` 응답 양식 포함. BACKLOG B12 `[DONE iter51]` 로 마킹, STATE iter50→51 (idle_upkeep_streak 1→2). 이 파일은 non-protected(.autopilot/ 루트, protected_paths 는 PROMPT/MVP-GATES/project.* 뿐) 이므로 PR 없이 main 직접 커밋. 테스트·빌드 미실행(해당 없음), 81/81 유지. 남은 자동 처리 후보는 iter52 에 B11(EventHashStamper 순수 리팩터) 또는 B13(대시보드 ASCII 히스토그램) 으로 이어갑니다.

(이전) **iter50 — idle-upkeep. BACKLOG 정리 + 신규 brainstorm 5건 추가.** MVP 7/8 달성 이후 로봇이 자동으로 밀 수 있는 코드 게이트는 전부 소진, G1 만 운영자 3-결정 대기 중이라 이번 iter 는 코드 변경 없는 **정리 턴**으로 운영. ① `.autopilot/BACKLOG.md` 를 현실에 맞게 전면 재작성 — 완료된 B2·B4·B5·B6·B7·B9 는 "Completed" 구간으로 분리, 남은 active 큐(B1·B3·B10·B8) 에는 각각 블록 사유 명시. ② **신규 brainstorm 5건 적재**: **B11** RelayBroker.cs 내 55개 `new RelayLogEvent(...)` 호출부를 `EventHashStamper.Stamp(...)` 래퍼로 통합해 나중에 해시 정책 바뀔 때 한 곳만 수정하도록 하는 순수 리팩터, **B12** G1 3-결정을 한국어 paragraph로 풀어쓴 `G1-UNBLOCKING-RUNBOOK.md` 작성(운영자가 편한 시간에 읽고 지시 가능), **B13** METRICS.jsonl 을 파싱해 대시보드 하단에 ASCII 히스토그램(일별 누적 머지 + MVP flip timeline), **B14** `tools/run-smoke.ps1` 신설 필요 여부 ADR(이미 xunit G4~G8 e2e 가 같은 범위를 커버하므로 재검토), **B15** MVP-GATES.md flip-history 한 줄 요약 표. ③ BACKLOG 는 gitignore 대상이라 파일 변경은 로컬 상태에만 반영, 추적 대상인 STATE + 대시보드 만 커밋 대상. 코드·테스트 무변경 → 빌드/테스트 돌릴 게 없음, 81/81 유지. 남은 G1 은 여전히 운영자 지시 대기 — 위 "관리자님께 드리는 제안" 섹션 3가지 중 하나 주시면 즉시 재개.

(이전) **iter49 — G8(감사 로그 무결성) 게이트 `[ ]`→`[x]` 완주 · MVP 7/8 달성!** 🎉🎉🎉 PR #50 자동 머지(769274a) + MVP-GATES.md 직접 main(61a55a4) G8 플립. 3-iter 스프린트 완결: (iter47) CanonicalHash 순수 함수 규칙 동결, (iter48) 모든 JSONL 이벤트에 `event_hash` 64자 지문 자동 스탬프, (iter49) 이번 PR 로 실제 동작 증명 — ① 같은 핸드오프를 브로커에 두 번 넣으면 \"Duplicate handoff detected\" 로 거부되고 수락 키 목록이 늘어나지 않음을 리플렉션으로 `State.CurrentTurn/ActiveAgent` 를 강제 롤백한 뒤 재호출해서 확인 + ② 이벤트 작성기를 `using` 블록에 가두어 스코프 종료(프로세스 crash 대리) 후 파일 재오픈으로 2줄 모두 살아있음과 두 줄의 `event_hash` 가 동일한 64자 hex 임을 확인. 전체 **81/81 통과**(+2). iter25 이후 G3·G7·G4·G5·G6·G8 **여섯 게이트 연속 완주**. 이제 MVP **7/8**, 남은 G1 은 운영자님 3가지 결정만 기다립니다(위 섹션).

(이전) **iter48 — G8 배선 완료. 이제 이벤트 로그의 모든 줄에 SHA-256 지문이 자동으로 찍힙니다.** PR #49 자동 머지(a4e103d). 쉽게 말하면: 브로커가 매 턴마다 남기는 이벤트 로그(`*.jsonl` 파일, 한 줄에 한 사건) 각 줄에 이제 `event_hash` 라는 **64자짜리 지문**이 같이 기록됩니다. 이 지문은 세션ID + 타임스탬프 + 이벤트 종류 + 주체(codex/claude) + 메시지 + 페이로드를 정해진 형식으로 묶어 SHA-256 해시한 값이라, 로그 한 줄을 나중에 누가 손대면 즉시 \"원본과 달라졌다\" 를 판별할 수 있게 됩니다. 기존 55개 생성자 호출부는 한 줄도 수정 안 했습니다 — 새 필드를 trailing 옵션 파라미터로 추가했기 때문. 이미 해시가 들어있으면 재계산 없이 보존하도록 해서, 다음 단계(iter49) 에서 브로커가 \"이 패킷 예전에 본 것과 해시가 같으니 무시\" 라는 replay-dedup 판정에 곧장 쓸 수 있게 준비했습니다. 테스트 6개로 지문 생성·동일성·세션 분기·preset 보존 등을 전부 검증 — 전체 **79/79 통과**(+6). **다음 iter49** 에서 \"같은 핸드오프 패킷을 브로커에 두 번 넣어도 상태 전이는 딱 한 번만 일어난다\" 를 xunit e2e 로 증명하면 G8 `[ ]`→`[x]` 로 MVP **7/8** 달성. 남은 게이트는 G1(운영자 3-질문 블록) 하나.

(이전) **iter47 — G8(감사 로그 무결성) 기초 블록 착륙.** PR #48 자동 머지(b5f2dfd). G8 목표는 세 가지입니다 — ① 모든 턴 패킷·핸드오프·상태 전이 이벤트에 **SHA-256 지문** 찍기, ② 같은 패킷을 두 번 넣어도 상태 전이는 **한 번만**(dedup), ③ 이벤트 로그가 프로세스가 죽어도 살아남기. 이걸 만들려면 먼저 "같은 내용이면 같은 지문" 규칙이 필요합니다. 이번 PR 이 딱 그 규칙 — `CanonicalHash.OfString(...)` 순수 함수 — 을 심었습니다. 줄바꿈(CRLF/LF/CR), 줄 끝 공백·탭, 파일 끝 빈 줄 따위의 포맷 차이는 싹 정규화한 뒤 SHA-256 해시를 내서 소문자 64자 hex 로 돌려줍니다. 정규화 규칙은 **얼어있습니다** — 나중에 바꾸면 과거 해시가 전부 무효화되니까요. 테스트 9개로 동등성(LF=CRLF=CR) · 멱등성 · 빈 문자열 표준 해시값 등을 전부 확인. 전체 **73/73 통과**(+9). 기존 코드 변경 0줄, 호출자 0, protected_paths 미접촉. **다음 iter48**에서 `RelayLogEvent` 에 `event_hash` 필드를 추가하고 `JsonlEventLogWriter` 가 이 해시를 찍도록 배선합니다. 그 다음 **iter49** 에서 "같은 패킷 두 번 → 전이 한 번" e2e 테스트를 붙이면 G8 `[ ]`→`[x]` 로 MVP **7/8** 이 됩니다.

(이전) **iter46 — G6 게이트 `[~]`→`[x]` 완주! G-BUNDLE 3-iter 스프린트(G4·G5·G6) 완결!** 🎉🎉 PR #47 자동 머지(a3ba00a). `BrokerRotationSmokeE2ETests` 1 fact — "대화가 길어져 한 번 쉬어갈 때 요약 쪽지를 잘 만들어 다음 차례 에이전트한테 전달하는가"를 end-to-end로 증명. 절차: Codex→Claude peer_handoff 한 번으로 State에 목표/완료/대기 상태를 시드 → 브로커의 private `RotateSessionAsync`를 리플렉션으로 호출 → `%LOCALAPPDATA%/CodexClaudeRelayMvp/summaries/{세션}-segment-1.md` 실제 파일 디스크 landing 확인 + `summary.generated` 이벤트 정확히 1회(segment·path·bytes 페이로드) + `CarryForwardPending=true` 플래그 검증 → 다음 `AdvanceAsync` 호출 → Claude 어댑터가 받은 `context.CarryForward` 안에 `## Carry-forward` / `- goal: finish the G6 rotation smoke` / `### Completed` / `### Pending` 블록이 정확히 주입되어 있음 + `summary.loaded` 이벤트 emit 확인. 기능 코드 0줄, 테스트 파일 157 LOC 신규만. `BrokerCwdMutating` 컬렉션으로 CWD 경합 차단. 테스트 **64/64 통과**. MVP **6/8** 달성 — **iter25 이후 최대 규모 연속 완주**(G3 이후 G4·G5·G6·G7 연달아 `[x]`). 남은 게이트는 G1(운영자 블록, 3가지 결정 대기) + G8(audit log integrity, 신규 역량). 다음 iter47은 G8 스캐폴딩 또는 BACKLOG B4-B10 중 선택.

(이전) **iter45 — G5 게이트 `[~]`→`[x]` 완주!** 🎉 PR #46 자동 머지(162c0ec). `BrokerRecoveryResumeE2ETests` 2 facts — turn 1이 `closeout_kind: recovery_resume`로 끝나면 broker가 `session.recovery_resume` 이벤트를 발행하고 다음 턴 prompt 앞에 "세션 재개 프리앰블"(`.prompts/04-session-recovery-resume.md` 참조 + `continued_from_resume: true`로 표시하라는 지시 + 원본 prompt)을 자동 prepend. `CapturingAdapter`로 다음 peer가 실제로 그 프리앰블을 **수신**하는지 intercept & assert → turn 2에서 peer_handoff 정상 수락 → `CurrentTurn=3` + `turn-2.yaml`/md landing. Codex-first·Claude-first 대칭. recovery_resume turn은 `HandoffArtifactWriter`가 거부(기존 의도된 행동; peer_handoff만 렌더)이므로 turn-1 아티팩트는 디스크 미landing — 본 게이트 범위 밖. 테스트 **63/63 통과**. MVP **5/8** 달성(G2·G3·G4·G5·G7). G-BUNDLE 스프린트 **2/3 완료** — 다음 iter46은 G6 rotation smoke.

(이전) **iter44 — G4 게이트 `[~]`→`[x]` 완주!** 🎉 PR #45 자동 머지(74686ef). `BrokerRoutingRoundTripE2ETests` 2 facts 작성 — `CannedAdapter` 쌍이 미리 준비된 `dad_handoff` JSON을 반환하도록 세팅하고 `broker.AdvanceAsync`를 두 번 호출하면 **실제 routing flow 전체**(파서 → State 갱신 → 아티팩트 쓰기)가 돌아가는지 검증. 결과: `Document/dialogue/sessions/{sid}/` 밑에 `turn-1.yaml`, `turn-2.yaml`, `turn-1-handoff.md`, `turn-2-handoff.md`, `state.json`(current_turn=3, active_agent=codex) 전부 실제 파일로 landing + `ActiveAgent`가 Codex↔Claude 교대. Codex-first·Claude-first 대칭 둘 다 OK. 부수로 `BrokerCwdMutatingCollection` 도입해 CWD 변경하는 e2e 테스트들(G4·G7) 직렬화 → 병렬 race 해소. 테스트 **61/61 통과**(59+2). MVP **4/8** 달성(G2·G3·G4·G7). G-BUNDLE 스프린트 1/3 완료 — 다음 iter45는 G5 recovery_resume e2e.

(이전) **iter43 — G4/G5/G6 `[x]` 번들 플랜 작성.** `.autopilot/G-BUNDLE-PLAN.md`에 3-iter 로드맵 확정: iter44 G4 브로커 routing e2e(CannedAdapter 쌍으로 turn-1/2.yaml 왕복) → iter45 G5 recovery_resume e2e(continued_from_resume 실증) → iter46 G6 rotation smoke(summary.generated + carry-forward 주입). iter42에서 확보한 fake-adapter 패턴(NoopAdapter + InMemory* fakes + 리플렉션 호출)을 세 테스트 파일에서 각각 독립 재사용(premature abstraction 금지 원칙). 성공 시 MVP **3/8 → 6/8**. 코드 변경 없음, 계획 문서만. 다음 iter44부터 실제 코드 착수.

(이전) **G7 게이트 완주 — `[~]`→`[x]` 플립!** 🎉 PR #44 자동 머지 + MVP-GATES.md 증거 스택 확정. iter42에서 실제로 e2e 돌려보니 **iter41 배선에 필드 구멍**이 있었음을 발견: `HandoffEnvelope`에 `suggest_done` / `done_reason` 필드가 아예 없어서 `ConvergenceDetector`가 영원히 발화 불가능한 상태였어요. 두 필드 + `TurnPacketAdapter` 매핑을 추가한 뒤, fake-adapter 기반 `BrokerConvergenceE2ETests` 2 facts 작성: (a) Codex→Claude 합의 한 쌍 흘리면 `State.Status=Converged` + `session.converged` + `backlog.closure_written` 이벤트 + `Document/dialogue/backlog.json` 실제 파일에 session_id/status/closed_by/converged_turn 모두 정확히 landing, (b) Claude→Codex 역순도 대칭적으로 동일. 테스트 **59/59 통과**. 이제 **MVP 게이트 3개 완주(G2·G3·G7)** + 3개 부분(G4·G5·G6). iter25 이후 **첫 신규 `[x]` 게이트**이자 **5-iter 스프린트(iter38~42)** 완결. 다음 iter43은 G8 audit log integrity 계획 수립 OR G4/G5/G6 [x] 번들 follow-up 선정.

---

## 📊 지금까지 한 일 (최근 5개 iteration)

| 회차 | 날짜 | 모드 | 결과 | 한국어 요약 |
|------|------|------|------|-------------|
| 64 | 04-19 | Active | **PR #54 MERGED** · G1 3/4 step 1 | IAgentCostAdvisor scaffold (pair registry + 2 no-op 훅 · 5 tests) · 자동 머지(58b6974) · 89/89 · 누적 26건 |
| 63 | 04-19 | Active | **PR #53 open** · G1 2/4 | tools/Validate-Dad-Packet.ps1 (en 스크래핑, 242 LOC) · protected → 관리자 리뷰 대기 · iter64 B3 병행 예정 |
| 62 | 04-19 | Active | **PR #52 MERGED** · G1 1/4 | PacketIO Read/Write facade + 라운드트립 3건 (84/84) · 자동 머지(5e916d6) · 누적 25건 |
| 61 | 04-19 | Active | **🎉 G1 언블록** · 관리자 3결정 접수 | RUNBOOK 답변블록·OPERATOR-INDEX §2 ✅·STATE parked→active_task_g1 · iter62 부터 B1/validator/B3/CLAUDE.md 4-step |
| 60 | 04-19 | Active | **PR #51 MERGED** 🎉 · B15 DONE | MVP-GATES Flip digest main 착륙(f21b617) · 누적 24건 · OPERATOR-PENDING-INDEX §1 ✅ |
| 59 | 04-19 | 리뷰대기+개선 | PR #51 폴링 5회(변화X) + **OPERATOR-PENDING-INDEX.md** | 4개 대기 항목(PR #51 · G1 3결정 · B14 · B10) 한 페이지 통합 · 🔴🟠🟡 우선순위 |
| 58 | 04-19 | 리뷰대기+개선 | PR #51 폴링 4회(변화X) + **B10 spec 착륙** | DECISION-BLOCKED-STATE.md · 3 규칙(R-BLOCKED/R-REVIEW/R-DRIFT) + 회고 검증 · 응답 양식 포함 |
| 57 | 04-19 | 리뷰대기+개선 | PR #51 폴링 3회(변화X) + **B13.1 regen script** | `.autopilot/regen-histogram.py` stdlib 65 LOC · 실행 검증 · exit-contract 훅 확장점 |
| 56 | 04-19 | 리뷰대기 | PR #51 폴링 2회 | 상태·댓글 변화 없음 · halt 대상 아님(정책 차단) |
| 55 | 04-19 | 리뷰대기 | PR #51 폴링 1회 | 상태 변화 없음 · reviewDecision 빈 값 · 관리자 리뷰 대기 |
| 54 | 04-19 | 리뷰대기 | **PR #51 생성 · B15 Flip digest** | MVP-GATES.md 상단에 8게이트 요약 표(+19줄) · protected → 관리자 리뷰 필수 · B11 CLOSED(중복) · 코드 무변경 |
| 53 | 04-19 | Idle-upkeep | **진행 히스토그램 + flip 연표 착륙** | B13 DONE · 대시보드 하단 신설 · 6h 버킷 ASCII 히스토그램 + G2~G8 flip 표 · 코드 무변경 · 81/81 유지 |
| 52 | 04-19 | Idle-upkeep | **ADR-TOOLS-SMOKE-NEED.md 착륙** | B14 세 옵션(A/B/C) 대비표 · Option A(5 LOC 래퍼) 권고 · 응답 양식 포함 · B14 DONE · 코드 무변경 · 81/81 유지 |
| 51 | 04-19 | Idle-upkeep | **G1-UNBLOCKING-RUNBOOK.md 착륙** | 관리자용 한국어 결정서(validator a/b/c · 비용 d/e/f · 승인/보류) · 응답 양식 포함 · B12 DONE · 코드 무변경 · 81/81 유지 |
| 50 | 04-19 | Idle-upkeep | BACKLOG 재스코어링 + brainstorm 5건 | 완료 항목(B2·B4·B5·B6·B7·B9) Completed 구간 분리 · 신규 B11~B15 적재 · 코드/테스트 무변경 · 81/81 유지 · 누적 23건 |
| 49 | 04-19 | Active | **PR #50 머지 · G8 `[x]` 완료 · MVP 7/8** 🎉🎉🎉 | replay-dedup + crash-survival e2e 2 facts · AcceptedRelayKeys 불변 증명 · 81/81 · 누적 23건 · **7/8 MVP** |
| 48 | 04-19 | Active | **PR #49 머지 · G8 step 2/3 배선** | RelayLogEvent.event_hash + Writer stamp 6 facts · 55개 호출부 무수정 호환 · 79/79 · 누적 22건 |
| 47 | 04-19 | Active | **PR #48 머지 · G8 기초 착륙** | CanonicalHash 순수 함수 9 facts · 정규화 규칙 동결 · 73/73 · 누적 21건 · MVP 6/8 유지 |
| 46 | 04-19 | Active | **PR #47 머지 · G6 `[x]` 완료 · G-BUNDLE 완주** 🎉🎉 | rotation smoke e2e 1 fact · summary file + summary.generated + carry-forward 주입 실증 · 64/64 · 누적 20건 · **6/8 MVP** |
| 45 | 04-19 | Active | **PR #46 머지 · G5 `[x]` 완료** 🎉 | G-BUNDLE 2/3: recovery_resume e2e 2 facts · preamble 전달 실증 · 63/63 · 누적 19건 · **5/8 MVP** |
| 44 | 04-19 | Active | **PR #45 머지 · G4 `[x]` 완료** 🎉 | G-BUNDLE 1/3: broker routing e2e 2 facts + CWD collection · 61/61 · 누적 18건 · **4/8 MVP** |
| 43 | 04-18 14:51 | Active | G-BUNDLE 계획 | G4/G5/G6 `[x]` 번들 3-iter 로드맵 확정 · 코드 변경 없음 · 누적 머지 17건 |
| 42 | 04-18 14:44 | Active | **PR #44 머지 · G7 `[x]` 완료** 🎉 | G7 step 4/4: e2e 스모크 2 facts + envelope 필드 보강(suggest_done/done_reason) · 59/59 · 누적 17건 · **3/8 MVP** |
| 41 | 04-18 14:38 | Active | **PR #43 머지 · G7 `[~]` 플립** | G7 step 3/4: 브로커 wiring + session.converged + backlog.closure_written · 57/57 · 누적 16건 |
| 40 | 04-18 14:30 | Active | **PR #42 자동 머지** | G7 step 2/4: BacklogClosureWriter upsert+render+atomic write 5 facts · 57/57 · 누적 15건 |
| 39 | 04-18 14:24 | Active | **PR #41 자동 머지** | G7 step 1/4: ConvergenceDetector 7 facts · Converged enum · 52/52 · 누적 14건 |
| 38 | 04-18 14:20 | Active | G7 계획 | G7-PLAN.md 작성 · 4-iter 로드맵(detector→writer→wiring→e2e) · 코드 변경 없음 · 누적 머지 13건 |
| 37 | 04-18 14:08 | Active | **G6 `[ ]`→`[~]` 플립** | 증거 스택 MVP-GATES 기록 · 코드 변경 없음 · 누적 머지 13건 |
| 36 | 04-18 14:04 | Active | **PR #40 자동 머지** | G6 step 1/3: Renderer+Writer xunit 8 facts · 45/45 · 누적 13건 |
| 35 | 04-18 14:00 | Active | G6 계획 | G6-PLAN.md 작성 · 기존 인프라 발견 · 3-iter 로드맵 · 코드 변경 없음 · 누적 12건 |
| 34 | 04-18 13:56 | Active | **G5 `[ ]`→`[~]` 플립** | 증거 스택 MVP-GATES 기록 · 코드 변경 없음 · 누적 머지 12건 |
| 33 | 04-18 13:52 | Active | **PR #39 자동 머지** | G5 step 2/3: 브로커 recovery_resume 분기 · 37/37 · 누적 12건 |
| 32 | 04-18 13:48 | Active | **PR #38 자동 머지** | G5 step 1/3: HandoffEnvelope.closeout_kind 필드 · 34/34 · 누적 11건 |
| 31 | 04-18 13:44 | Active | G5 계획 | G5-PLAN.md 작성 · 3-iter 로드맵 · 코드 변경 없음 · 누적 머지 10건 |
| 30 | 04-18 13:42 | Active | **G4 `[ ]`→`[~]` 플립** | 증거 스택 MVP-GATES 기록 · 코드 변경 없음 · 누적 머지 10건 |
| 29 | 04-18 13:39 | Active | **PR #37 자동 머지** | G4 step 3/4: 라운드트립 스모크 3 facts · 33/33 · 누적 10건 |
| 28 | 04-18 13:35 | Active | **PR #36 자동 머지** | G4 step 2/4: 브로커 hook + state.json · 30/30 · 누적 9건 |
| 27 | 04-18 13:32 | Active | **PR #35 자동 머지** | G4 step 1/4: 턴 패킷 YAML persister · 27/27 · 누적 8건 |
| 26 | 04-18 13:26 | Active | G4 계획 | G4-PLAN.md 작성 · 4-iter 로드맵 확정 · 코드 변경 없음 |
| 25 | 04-18 13:25 | Active | **PR #34 자동 머지 · G3 [x]** | 증거 없는 체크포인트 턴 차단 enforcement · 21/21 · 누적 7건 |
| 24 | 04-18 13:20 | Active | **PR #33 자동 머지** | G3 브로커 hook + xunit 6/6 · G3 [ ]→[~] · 누적 머지 6건 |
| 23 | 04-18 13:19 | Active | G3 스캐폴드 커밋 | TurnPacket.PeerReview + CheckpointVerifier 순수 함수 |
| 22 | 04-18 13:12+ | Active | PR #32 머지 확인 | 자동 머지 규칙 발효 · 다음 MVP 게이트 선정 진입 |
| 21 | 04-18 13:14 | 리뷰대기 | PR #32 폴링 1회 | 상태 변화 없음 |
| 20 | 04-18 13:06+ | Active | **G2 [x] 완료** | PR #31 머지 확인, MVP-GATES 이력 기록, 다음 gate 선정 진입 |
| 17-19 | 04-18 | Active→리뷰대기 | PR #31 생성·폴링 | G2-WIRE 3/3 · xunit 3/3 통과 · 3회 폴링 후 머지 |
| 15-16 | 04-18 | Active | G2-WIRE 1-3/3 | Persister + Adapter + broker hook + broker→Persister 연결 |
| 14 | 04-18 12:48+ | Active | PR #30 머지 | 한국어 대시보드·PR 규칙 착륙 |
| 12-13 | 04-18 | 리뷰대기 | PR #30 폴링 | 상태 변화 없음 (2회) |
| 11 | 04-18 07:50 | 자기진화 | PR #30 생성 | 관리자(한국어)를 위한 대시보드·PR규칙 추가 |
| 10 | 04-18 07:35 | 대기 | 🛑 정지 (halt-soft) | PR 검토가 안 돼서 자동 정지 (지금은 해결됨) |
| 9 | 04-18 07:11 | 대기 | 진전 없음 | PR #28, #29 검토 대기 중 (idle 2/3) |
| 8 | 04-18 06:51 | 자기진화 | PR #29 생성 | "리뷰 대기 모드" 신설 |
| 7 | 04-18 06:21 | 대기 | 진전 없음 | G2 broker 연결 계획서 작성 |

**누적 머지:** 23건 (iter5~iter49)
**누적 빌드 상태:** 항상 clean (0 오류)

---

## 📈 진행 히스토그램 (METRICS.jsonl 파생 · iter53 자동 생성)

**6시간 구간별 머지 + 게이트 flip** — `#` 하나 = PR 머지 1건.

```
04-18 00h  iters= 7  merges= 0
04-18 06h  iters= 6  merges= 0
04-18 12h  iters=31  merges= 7  #######        ← G7 [~]→[x] (iter42)
04-19 00h  iters= 9  merges= 6  ######         ← G4, G5, G6, G8 [x] 연속 (iter44~49)
04-19 00h~ iters= 4  merges= 0                 ← iter50~53 idle-upkeep (B12·B14·B13 문서)
```

**게이트 flip 연표** (METRICS 에 `gate_flip` 필드로 찍힌 것만):

| iter | 시각 (UTC) | flip |
|------|-----------|------|
| 20 | 2026-04-18 13:06 | G2 `[ ]`→`[x]` (PR #31) |
| 25 | 2026-04-18 13:25 | G3 `[ ]`→`[x]` (PR #34) |
| 42 | 2026-04-18 14:44 | G7 `[~]`→`[x]` (PR #44) |
| 44 | 2026-04-19 00:00 | G4 `[~]`→`[x]` (PR #45) |
| 45 | 2026-04-19 00:15 | G5 `[~]`→`[x]` (PR #46) |
| 46 | 2026-04-19 00:30 | G6 `[~]`→`[x]` (PR #47) |
| 49 | 2026-04-19 01:15 | G8 `[ ]`→`[x]` (PR #50) |

**관찰**: 04-18 오후(12h 구간) 에 G7 1건, 그리고 04-19 자정 이후 G4·G5·G6·G8 **연속 4 flip** — 핵심
e2e 인프라(`BrokerCwdMutatingCollection`, fake-adapter 패턴)가 iter42 에 확보되자 이후 3~5 iter
스프린트가 안정적으로 착륙. iter50 부터는 코드 게이트 고갈 → idle-upkeep 문서 턴.

---

## 🚧 로봇이 관리자님께 물어보는 것 4건 (급하지 않음)

로봇이 혼자 결정하면 위험한 4가지를 대기 중입니다. 개발자분과 상담 기회가 생기시면 전달만 부탁드려요.
(세부 내용은 `.autopilot/STATE.md`의 `operator_requests` 섹션.)

1. **G1 검증 스크립트** — 어떤 방식으로 포팅할지
2. **Codex/Claude 비대칭 비용 경로** — 제거할지, 대칭화할지
3. **옛날 템플릿 잔재 파일** — 삭제해도 될지
4. **G1 우회 평가 경로** — 대체 기준을 인정할지

관리자님이 판단하기 어려우시면 **그냥 두셔도 됩니다.** 로봇은 이 4개가 막혀도 다른 8+ 개의 백로그로 우회해서 계속 일할 수 있어요.

---

## 🗝 관리자님이 보낼 수 있는 명령 (STATE.md에 `OPERATOR:` 라인 추가)

긴급할 때만 사용하세요. 평소에는 그냥 머지만 해주시면 됩니다.

| 명령 | 언제 쓰나요 |
|------|-------------|
| `OPERATOR: halt` | 🛑 로봇을 즉시 멈출 때 |
| `OPERATOR: 리뷰 필수` (= require human review) | 자동 머지를 끄고 싶을 때 |
| `OPERATOR: 정리` (= run cleanup) | 오래된 파일 정리가 필요할 때 |

---

## 📂 관리자가 알면 좋은 파일 (참고용, 안 열어도 됨)

- 이 파일 — 한국어 대시보드 (로봇이 매번 갱신)
- `.autopilot/HISTORY.md` — 영문 상세 로그 (로봇용)
- `.autopilot/STATE.md` — 현재 상태 변수 (로봇용)
- `.autopilot/LAST_HALT_NOTE` — 로봇이 멈췄을 때 이유 (있을 때만)
