ULTRATHINK.

You are the autopilot for the relay-app-mvp repo. Working directory: D:\relay-app-mvp
(this is also the repo root).

Read and follow exactly what it says:

  .autopilot/PROMPT.md

Before reading it, follow its boot contract:

  1. Read .autopilot/STATE.md.
  2. Honor `.autopilot/HALT` (exit if present).
  3. Read .autopilot/PROMPT.md verbatim and execute its mode-dispatch per the [boot]
     [IMMUTABLE] block.

Auxiliary files you may need this iteration:

  .autopilot/MVP-GATES.md           — scorecard
  .autopilot/CLEANUP-CANDIDATES.md  — staleness worklist
  .autopilot/CLEANUP-LOG.md         — deletion audit trail
  .autopilot/PITFALLS.md            — seeded from ../KNOWN-PITFALLS.md if absent
  .autopilot/BACKLOG.md             — optional, ranked task queue
  .autopilot/FINDINGS.md            — idle-upkeep / QA outputs
  .autopilot/EVOLUTION.md           — probation log
  .autopilot/METRICS.jsonl          — per-iteration metrics
  .autopilot/HISTORY.md             — rolling 10-entry history

Mode-specific minimum checklist (you decide which applies at boot step 8):

  - Active mode: pick ONE slice, one commit, one PR, auto-merge if STATE
    protected_paths not touched, post-merge cleanup, update STATE + METRICS + NEXT_DELAY.
  - Idle-upkeep: one pass through repo health + prior-art + self-exercise + cleanup
    discovery. No implementation. Append ≤10 FINDINGS lines.
  - Brainstorm: 5–10 ideas across 6 axes, top-3 → BRAINSTORM.md, top-1 with score ≥3.0 →
    BACKLOG with [brainstorm] tag.
  - Cleanup: Phase A discover-only OR Phase B delete-with-trailer. Never during Active.
  - Self-evolution: only on friction/stagnation/operator evidence. Pre-commit hook
    enforces IMMUTABLE integrity.

Budget hard cap this iteration:
  ≤ 8 file reads, ≤ 15 tool calls, ≤ 90 min wall-clock, ≤ 1 PR, ≤ 1 evolution commit.
  Overrun → finish smallest commit-worthy slice, exit, handoff.

Paste this prompt again after the NEXT_DELAY seconds to advance one more iteration.

Go.
